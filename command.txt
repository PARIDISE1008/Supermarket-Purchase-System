mysql -u root -p --default-character-set=utf8mb4 < Database/schema.sql
mysql -u root -p --default-character-set=utf8mb4 < Database/data.sql

cd Frontend
npm run dev（前端）

cd BackendService
mvn spring-boot:run（后端）

http://localhost:3000

13800001001
13800001002

sudo systemctl start redis（启动redis）*

同步更新一下日志6.15.md和报告（报告保留原有的描述，修改与实际项目不符的地方）